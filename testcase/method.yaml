-
  casename: get请求
  request:
    url: ${base_url}/get
    method: GET
    params:
      foo1: bar1
      foo2: bar2
-
  casename: post请求data传参
  request:
    url: ${base_url}/post
    method: POST
    data:
      foo1: bar1
      foo2: bar2
-
  casename: post请求json传参
  request:
    url: ${base_url}/post
    method: POST
    json:
      foo1: bar1
      foo2: bar2
-
  casename: post请求files传参
  request:
    url: ${base_url}/post
    method: POST
    files:
      file1: system/img/allure.png

